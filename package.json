{
  "dependencies": {
    "dotenv": "^16.0.1",
    "express": "^4.18.1",
    "jsonwebtoken": "^8.5.1",
    "mongoose": "^6.3.3"
  },
  "scripts": {
    "setup": "npm i && cd client && npm i",
    "build": "cd client && npm run build",
    "client-dev": "cd client && npm start"
  },
  "type": "module"
}
